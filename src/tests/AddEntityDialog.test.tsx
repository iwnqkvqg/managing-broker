import { describe, expect, it } from "vitest";

describe("AddEntityDialog", () => {
  it("should render", () => {
    expect(true).toBeTruthy();
  });
  /*
  xit("should call `onSubmit` on primary action button click")
  xit("should call `onSubmit` on Enter press if a text field has focus")
  xit("should call `onClose` on Escape press")
  xit("should call `onClose` on click outside")
  xit("should call `onClose` on secondary action button click")
  xit("should not allow submitting incomplete form")
  */
});
