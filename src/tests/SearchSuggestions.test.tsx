import { describe, expect, it } from "vitest";


describe("SearchSuggestions", () => {
  it("should render", () => {
    expect(true).toBeTruthy();
  });
  /*
  onClick should select suggestion
  on add manually should open modal
  */
});